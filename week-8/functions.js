
// Functions - A JavaScript function is a block of code designed to perform a particular task.

function myFunction() {
    // code to be executed
    // do something
}

// basic arithmetics
function addNumbers () {
    let a = 7 + 5;
    console.log(a);
}

function subtractNumbers () {
    let b = 7 - 5;
    console.log(b);
}

function multiplyNumbers () {
    let c = 7 * 5;
    console.log(c);
}

// subtractNumbers();

function startACar (){
    // 1. Open the door
    // 2. Sit in the car
    // 3. Close the door
    // 4. Put the key in the ignition
    // 5. Turn the key to the right
    // 6. Press the gas pedal
    // 7. Release the handbrake
    // 8. Drive
}

let num = 7;

function addTwoNumbers (x, y) {
    let total = x + y;
    console.log("The output is " + total);
}

addTwoNumbers(7, 5);
addTwoNumbers(23, 8);
addTwoNumbers(43, 2);